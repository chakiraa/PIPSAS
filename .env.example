# Copy to .env.local for local development. Never commit real keys.

# Required for the AI assistant (/api/chat). Get a free key at
# https://aistudio.google.com/apikey — every other feature of the app
# works fine without this set.
GEMINI_API_KEY=

# Optional — defaults to gemini-2.5-flash if unset.
GEMINI_MODEL=
